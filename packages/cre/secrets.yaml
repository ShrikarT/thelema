# Logical secret names only. Values never live in this repository.
# Simulation reads MAX_NOTIONAL_ENV from the shell or an untracked .env.
# Deployed workflows receive MAX_NOTIONAL through `cre secrets` and the Vault DON.
secretsNames:
  MAX_NOTIONAL:
    - MAX_NOTIONAL_ENV
